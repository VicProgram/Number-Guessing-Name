https://roadmap.sh/projects/number-guessing-game

Requirements
It is a CLI-based game, so you need to use the command line to interact with the game. The game should work as follows:

When the game starts, it should display a welcome message along with the rules of the game.

The computer should randomly select a number between 1 and 100.

User should select the difficulty level (easy, medium, hard) which will determine the number of chances they get to guess the number.

The user should be able to enter their guess.

If the user's guess is correct, the game should display a congratulatory message along with the number of attempts it took to guess the number.

If the user's guess is incorrect, the game should display a message indicating whether the number is greater or less than the user's guess.

## The game should end when the user guesses the correct number or runs out of chances.

JUST RUN THIS PYTHON FILE

Requisitos

Es un juego basado en la línea de comandos (CLI), por lo que debes usar la terminal para interactuar con él. El juego debe funcionar de la siguiente manera:

Cuando el juego comience, debe mostrar un mensaje de bienvenida junto con las reglas del juego.

La computadora debe seleccionar aleatoriamente un número entre 1 y 100.

El usuario debe seleccionar el nivel de dificultad (fácil, medio, difícil), lo cual determinará la cantidad de intentos que tendrá para adivinar el número.

El usuario debe poder ingresar su suposición (número).

Si el número ingresado por el usuario es correcto, el juego debe mostrar un mensaje de felicitación junto con la cantidad de intentos que le tomó adivinarlo.

Si el número es incorrecto, el juego debe mostrar un mensaje indicando si el número secreto es mayor o menor que la suposición del usuario.

El juego debe terminar cuando el usuario adivine el número correctamente o se quede sin intentos.
